const express = require("express");
const router = express.Router();
const schema = require("../../models/schema");

router.get(
  "/users.register",
  (async = (req, res, next) => {
    console.log("token");
  })
);
